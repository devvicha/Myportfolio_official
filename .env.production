# Production build config. The Cloud Run URL is public, not a secret.
# Committed deliberately so a rebuild can never silently ship localhost again.
VITE_ASK_API_URL=https://portfolio-agent-backend-766118563029.us-central1.run.app
